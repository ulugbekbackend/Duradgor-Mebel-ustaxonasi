# Frontend konteyneri: SPA — mavjud bo'lmagan yo'llar index.html ga (BrowserRouter uchun)
server {
    listen 80;
    root /usr/share/nginx/html;

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }
}
